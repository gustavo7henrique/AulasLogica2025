//Exercício 01
const prompt = require( "prompt-sync" ) ();
////////////////////////////////////////////////////////////

for(var impares = 1; impares <= 40; impares++ ) {
    if (impares % 2 != 0) {
        console.log(`${impares} é um nº ímpar`);
        
    }
};
////////////////////////////////////////////////////////////